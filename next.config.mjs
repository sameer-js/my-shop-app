/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'firebasestorage.googleapi.com',
      'm.media-amazon.com',
      'cdn.dummyjson.com',
      'images.pexels.com',
    ],
  },
};

export default nextConfig;
